namespace Bindings
{
    // Server to Client
    public enum ServerPackets
    {
        SConnectionOk = 1
    }

    // Client to Server
    public enum ClientPackets
    {
        CThankyou = 1
    }
}